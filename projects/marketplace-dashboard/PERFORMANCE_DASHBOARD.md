# Производительность Пульта — 25 сентября 2026

## Актуальная read-only проверка (18:53–18:55 МСК)

Скрипт `scripts/perf-smoke.cjs` сделал по два последовательных HTTP GET к семи запрошенным маршрутам и к полному `insights`. Время ниже — **первый запрос / повторный запрос**, не статистический p95; первый запрос не гарантированно холодный. Значения взяты из недавнего `/api/runtime-metrics`, размер тела дополнительно сверялся с HTTP. Все ответы — 200, внешних API-вызовов — 0.

| Endpoint | HTTP, мс | SQL, мс / запросы | Pool wait, мс | Payload |
| --- | ---: | ---: | ---: | ---: |
| `/api/business-dynamics` | 50 / 38 | 39 / 3 → 29 / 3 | 0,2 / 0,2 | 208 919 Б |
| `/api/stores` | 9 / 5 | 6 / 7 → 3 / 7 | 0,2 / 0,2 | 1 912 Б |
| `/api/insights?scope=orders` | 170 / 19 | 35 / 14 → 21 / 14 | 598 / 2 | 13 934 Б |
| `/api/insights?scope=full` | 986 / 878 | 265 / 14 → 206 / 14 | 4 / 3 | 444 691 Б |
| `/api/category-sales?days=7` | 126 / 144 | 155 / 7 → 217 / 7 | 0,4 / 0,4 | 830 255 Б |
| `/api/b2b-radar?limit=50` | 2 138 / 14 | 2 122 / 3 → 1 / 2 | 0,1 / 0 | 272 895 Б |
| `/api/order-category-daily` (7 дней) | 1 487 / 69 | 455 / 69 → 29 / 5 | 411 / 0,3 | 2 200 679 Б |
| `/api/buyer-product-segments` (7 дней) | 3 066 / 4 | 1 702 / 4 → 1 / 2 | 0,1 / 0 | 23 145 Б |

Повторные B2B и product-segments быстрые, но первые чтения остаются тяжёлыми; эти результаты — прямое основание для следующего SQL-профиля именно этих холодных путей. Категории по дням отдают 2,2 МБ, значит, отдельная проверка payload/pagination остаётся оправданной. Не утверждаем ускорение «до/после» для этих маршрутов: перед этим этапом нет свежей сопоставимой baseline-серии с теми же параметрами.

Отдельно выполнена одна смешанная проба: два одновременных полных insights плюс бизнес-динамика и магазины. Динамика ответила за **79 мс на сервере / 3 SQL**, магазины за **54 мс / 6 SQL**; полные отчёты заняли около **2,14 с**. Пулы в снимке: runtime 10, UI 3, analytics 2, outbound 2; всего лимит **17 соединений**, в конце ожидание было 0. Внутри этой пробы UI-соединения ожидались примерно 43–60 мс на старте; это не доказывает, что фоновые Ozon/WB синхронизации присутствовали. Внешние API в отчётных GET не вызывались.

Перезагрузка вкладки через доступный браузерный инструмент не дала новых записей HTTP в метриках сервера; поэтому отдельные TTFB/FCP/LCP и достоверное browser navigation time здесь **не заявлены**. Инструмент показал уже доступное дерево текущего dashboard; это не замена замеру браузерной навигации.

Метрика `poolWaitMs` включает получение или установку соединения и суммируется по параллельным SQL-операциям; она может превышать wall time. Добавленное в исходники `otherMs` — только неотрицательный остаток `wall − SQL − acquire wait − external`, а не эксклюзивное CPU time. Работающий процесс не перезапускался, поэтому это поле в приведённых live-записях отсутствует.

## Свежий контроль после оптимизации (18:07 МСК)

Семь маршрутов измерены последовательными HTTP GET на работающем локальном сервере до изменения кода и после чистого перезапуска. Таблица показывает **первый запрос** каждой серии: полное время / суммарное SQL-время / число SQL / размер ответа. SQL-время может превышать полное время при параллельных запросах. Время зависит от фоновой синхронизации, поэтому эти единичные пробы не являются SLA.

| Endpoint | До | После | Результат |
| --- | ---: | ---: | --- |
| `/api/business-dynamics` | 122 / 51 мс / 3 / 173 811 Б | 58 / 25 мс / 3 / 173 811 Б | Бизнес-расчёт сохранён: перенос модели в API не давал доказанного выигрыша. |
| `/api/stores` | 14 / 10 мс / 7 / 1 853 Б | 12 / 7 мс / 7 / 1 905 Б | Уже короткий локальный маршрут. |
| `/api/insights` (полный) | 1 581 / 1 085 мс / 169 / 585 361 Б | 1 136 / 237 мс / 14 / 585 325 Б | Пакетное чтение снимков вместо N+1. |
| `/api/category-sales` | 7 153 / 19 620 мс / 212 / 832 439 Б | 2 547 / 2 446 мс / 8 / 832 439 Б | Чтение только нужных SQL-источников; одинаковый ответ. |
| `/api/b2b-radar` (`limit=100`) | 5 452 / 5 427 мс / 2 / 438 502 Б | 3 889 / 3 864 мс / 3 / 438 502 Б | Уменьшена сортировка; повторный запрос использует кеш по ревизии. |
| `/api/order-category-daily` | 28 499 / 15 968 мс / 88 / 1 761 100 Б | 2 487 / 1 324 мс / 32 / 1 761 100 Б | Узкое чтение категорий; повторный запрос 65 мс / 5 SQL. |
| `/api/buyer-product-segments` | 3 747 / 2 334 мс / 29 / 17 177 Б | 2 807 / 1 411 мс / 4 / 17 177 Б | Пакетная агрегация; повторный запрос 2,5 мс / 2 SQL. |

По 20 последовательным тёплым HTTP-пробам p95: категории продаж **155 мс**, B2B **26 мс**, категории по дням **103 мс**, покупатели **3 мс**, магазины **9 мс**. Полные insights — **1 023 мс**. У «Динамики бизнеса» p95 **431 мс**, хотя медиана **25 мс**: в нескольких пробах наблюдались ожидание соединения и конкуренция с фоном. Заявлять достижение цели p95 < 200 мс нельзя. В этой задаче новые индексы и постоянные агрегаты не добавлялись: планы и паритет ответов не доказали потребность. Redis/ClickHouse не добавлялись.

Отдельная серия из 40 последовательных HTTP-запросов «Динамики бизнеса» без параллельного полного insights дала клиентский p50 **31,6 мс**, p95 **38,6 мс**, максимум **77,7 мс**; SQL p95 **17,2 мс**, 3 запроса. `EXPLAIN ANALYZE` основного SQL: выполнение **13,0 мс**, используется существующий `live_facts_entity_order_idx`. При шести одновременных полных чтениях insights p95 динамики доходил до **863 мс** из-за конкуренции за PostgreSQL и UI-пул. Главная страница по умолчанию запрашивает лёгкий `scope=orders`; тяжёлый полный отчёт открывается в финансовых разделах или при выборе финансового показателя. Изолированный p95 ниже 200 мс не заменяет замер под реальной смешанной нагрузкой.

В браузере устранены повторные запросы insights при двух навигационных событиях; для графика категорий повторный запрос в течение 30 секунд и одновременные дубли объединяются. Начальная порция B2B уменьшена до 50 строк, продолжение загружается по запросу. На одинаковом периоде ответ сократился с 637 133 Б при 200 строках до 246 558 Б при 50 строках; KPI и покрытие совпали по SHA-256. Время отрисовки браузера и полный initial dashboard < 1 с в этой серии не измерялись.

## Текущий аудит и границы замера

Путь запроса: браузер (обычный JavaScript, без React) → локальный HTTP-сервер → PostgreSQL. Интерактивные данные читаются через `pult_ui` (максимум 3 соединения), тяжёлая SQL-аналитика — через отдельный `pult_analytics` (2), фоновые записи — через `pult` (10), Ozon HTTP — через `pult_ozon_http` (2). Совокупный лимит — 17. Открытие «Динамики бизнеса» не вызывает маркетплейсы напрямую. PostgreSQL остаётся источником истины; Redis и ClickHouse не требуются по текущим измерениям.

Снимок `GET /api/runtime-metrics` снят 25 сентября в 16:26 МСК со старого рабочего процесса. Это накопительные средние с запуска процесса, не p95. Суммарное SQL-время и ожидание пула могут превышать полное время запроса, если SQL выполнялись параллельно. Для маршрутов с 1–4 вызовами порядок ориентировочный. После разрешённого перезапуска в 16:37–16:41 МСК новый процесс подтвердил поля `p50Ms/p95Ms/p99Ms` на ограниченной выборке последних 128 запросов каждого маршрута.

| № | GET | Вызовов | Среднее, мс | SQL/вызов | Размер ответа | Причина / действие в этом этапе | После на HTTP |
| ---: | --- | ---: | ---: | ---: | ---: | --- | --- |
| 1 | `/api/category-sales` | 1 | 9 910 | 268 | 830 КБ | Холодное чтение нескольких источников; предыдущее параллельное чтение уже внедрено, нужен повторный профиль | не измерено |
| 2 | `/api/b2b-radar` | 1 | 8 017 | 2 | 1 967 КБ | Тяжёлый SQL/передача; требуется отдельный план и профиль payload | не измерено |
| 3 | `/api/order-categories` | 1 | 6 436 | 202 | 444 КБ | Холодные источники; на прошлом этапе внедрена узкая проекция | не измерено |
| 4 | `/api/order-category-daily` | 4 | 5 272 | 67 | 850 КБ | Холодная история и ожидание UI-пула | не измерено |
| 5 | `/api/buyer-product-segments` | 3 | 3 413 | 21 | 12 КБ | Агрегация больших исходных строк; предрасчёт без доказанной паритетности пока не вводился | не измерено |
| 6 | `/api/buyer-order-segments` | 3 | 2 020 | 2 | 6 КБ | Большое чтение/декодирование; нужен отдельный профиль | не измерено |
| 7 | `/api/data` | 971 | 913 | 18 | 44 КБ | Лишний опрос каждые 4 с, повторный registry read и перенос 167 189 операций; UI polling исправлен, операции агрегируются в SQL | три live GET: 352,5 / 367,1 / 317,9 мс / 17 SQL / 40 107 байт |
| 8 | `/api/insights` | 32 | 465 | 55 | 60 КБ | Разные режимы под одним путём; полный отчёт остаётся тяжёлым и вызывается по запросу | не измерено |
| 9 | `/api/insights/sources` | 1 | 222 | 37 | 453 КБ | Загрузка источников; без достаточной выборки не менялся | не измерено |
| 10 | `/api/stores` | 1 086 | 217 | 55 | 1,8 КБ | 10 полных чтений costs/prices на 5 магазинов; заменены пакетными ревизиями в реальном HTTP-core | 4,9 мс p50 / 7 SQL, 8 live GET; 1,9 КБ |
| 11 | `/api/ideas` | 58 | 192 | 1 | 8,6 КБ | Очередь HTTP под параллельной нагрузкой; собственный SQL короткий | не измерено |
| 12 | `/api/impact` | 58 | 186 | 1 | 96 Б | Очередь HTTP/БД под параллельной нагрузкой | не измерено |
| 13 | `/api/profit-series` | 3 | 162 | 15 | 3,6 КБ | Локальный кеш; выборка мала | не измерено |
| 14 | `/business-dynamics-view.js` | 58 | 90 | 0 | 49 КБ | Статический ресурс; пики от общей очереди/браузера, не SQL | не измерено |
| 15 | `/turnover-chart.js` | 58 | 89 | 0 | 69 КБ | Статический ресурс; пока без изменения | не измерено |
| 16 | `/profit-compare.js` | 58 | 88 | 0 | 21 КБ | Статический ресурс; пока без изменения | не измерено |
| 17 | `/economics-ui.js` | 58 | 88 | 0 | 13 КБ | Статический ресурс; пока без изменения | не измерено |
| 18 | `/sales-decline-ui.js` | 58 | 88 | 0 | 5,5 КБ | Статический ресурс; пока без изменения | не измерено |
| 19 | `/api/data-updates` | 18 | 78 | 5 | 6,5 КБ | Локальные статусы; пока без изменения | не измерено |
| 20 | `/api/wb/orders` | 19 | 66 | 10 | 600 Б | Локальные данные; пока без изменения | не измерено |

**Подтверждённое до/после в одинаковом read-only прямом вызове** `publicStores()` на рабочей БД, по 8 парных повторов: медиана **102,6 → 3,9 мс (26,5×)**, SQL **54–57 → 6**, максимум из восьми **181,9 → 6,6 мс**. После перезапуска отдельный живой HTTP-замер дал `/api/stores` **p50 4,9 мс / p95 59,5 мс / 7 SQL** на первых восьми запросах; на следующих двадцати запросах клиентская медиана была **5,2 мс**, p95 **9,6 мс**, максимум **130,5 мс**. Старый одиночный процесс имел среднее **217 мс / 55 SQL** за 1086 вызовов; это другая нагрузка, поэтому делить 217 на новую медиану и объявлять итоговое ускорение некорректно. В парной последовательной контрольной пробе до включения кода p50 был **107,3 мс** (8 запросов), после — **4,9 мс** (8 запросов), то есть **21,9× по p50 в малой пробе**. Ответы совпали по прежним полям; новый `snapshotRevision` появился у всех пяти магазинов и увеличил ответ до **1913 байт**. Первый запуск обнаружил, что HTTP-core создаётся отдельно от фонового core и не получил оптимизацию; это соединение исправлено и покрыто тестом до второго перезапуска.

**Повторные запросы UI:** в 21 проверенном стартовом сценарии две параллельные загрузки `/api/stores` сократились с 42 до 21 (−50%). Страница подключений ранее делала 847 `/api/data` за ~57 минут: сравнивала несопоставимые `completedAt` и `updatedAt`. Теперь при неизменных ревизиях детерминированный сценарий делает один начальный GET за это окно; при старом backend безопасный fallback — примерно один GET в 45 с. Это расчёт частоты по алгоритму и тестам, не измерение работающей версии.

**Фон:** длинный `ozon-performance` больше не удерживает в той же полосе `insights-today` и `wb-orders`. Тест удерживает первый job и доказывает запуск второго до его окончания. Реальная задержка после развёртывания ещё не измерена; следующий tick планировщика — до 30 с. Для успешного `/api/data` один повторный registry read убран: в парном прямом замере **10 → 9 SQL**; время отдельной пробы 901 → 740 мс зависит от текущей нагрузки. После первого перезапуска отдельный live GET `/api/data` подтвердил **790,7 мс / 17 SQL / 40 107 байт / 0 external API**.

**Дальнейшая точечная правка `/api/data`:** прямой read-only профиль выявил **167 189** финансовых операций, передаваемых из PostgreSQL в Node при каждом первом запросе. `GROUPING SETS` возвращает **34 строки** с теми же группами и днями; для нетипичных `amount` остаётся прежняя проекция. Полный JSON ответа проблемного магазина сравнен байт-в-байт с прежним путём, **40 107 байт** и одинаковый SHA-256. Пять прямых измерений дали прежний путь **703–1042 мс**, новый **314–353 мс**; `EXPLAIN (ANALYZE, BUFFERS)` для чтения операций **538,4 → 398,2 мс**, temp blocks read/write **4309/4317 → 2050/2055**. SQL count прямого вызова остаётся 15; выигрывает прежде всего меньшая передача и разбор строк. После второго перезапуска три живых HTTP-ответа составили **352,5 / 367,1 / 317,9 мс** при **17 SQL** и **0 внешних API**, с тем же SHA-256 ответа. Старый одиночный HTTP-ответ перед правкой был **790,7 мс / 17 SQL** на том же магазине; по лучшей из трёх новых проб это **2,5×**, но выборка мала и фон менялся.

**«Динамика бизнеса»:** три последовательных live GET `/api/business-dynamics` после включения версии ответили **170,8 / 31,9 / 31,2 мс** серверного времени, по **3 SQL** и **0 external API** каждый; тело ответа **205 834 байта**. Первый вызов холоднее последующих. Данных для сравнения p95 этого маршрута до и после пока нет.

**Разовая конкурентная проба `/api/stores` во время работающих фоновых задач:** 1/10/50 параллельных read-only GET завершились с HTTP 200. Для 10 запросов общий wall был **74 мс**, клиентский p95 **74 мс**; для 50 — общий wall **287 мс**, клиентский p95 **279 мс**. Отдельный одиночный GET в этой пробе занял **113 мс**, что показывает влияние текущего фона. Это одна проба локального компьютера, не нагрузочный SLA; по другим маршрутам выводов нет.

**Браузер:** теперь DevTools показывает FCP, LCP, INP и последние 20 переключений SPA-маршрутов через `window.PultPerformance.getMetrics()`; Performance Timeline получает `pult:route`. Метрики не отправляются на сервер. Route duration охватывает переключение оболочки до второго кадра отрисовки, но не ожидание API или окончательную отрисовку данных; реальные FCP/LCP/INP после перезапуска и открытия браузера ещё не сняты.

**PostgreSQL:** версия 18.6, база около 14 ГБ. На снимке UI-пул был занят 3/3 и ожидали 7–10 запросов; позднее ожидание исчезло. Блокировок и deadlock не найдено. CPU ~1,2% машины, свободная RAM ~25 ГБ — ресурсов достаточно, поэтому увеличение пула не выбрано как первый шаг. У крупных таблиц `last_analyze`/`last_autoanalyze` отсутствует; `pg_stat_statements` и `track_io_timing` выключены. Точный TOP SQL по `total_exec_time`, p95 SQL-запросов и причинность 47 ГБ накопленных temp files пока получить нельзя. `EXPLAIN` без `ANALYZE` показал Bitmap Heap Scan + сортировку по `pult_live.facts`, хотя индекс порядка уже существует. Новые индексы и изменение настроек не обоснованы до обновления статистики и повторного `EXPLAIN ANALYZE`; миграций, агрегатных таблиц и внешнего кеша в этом этапе нет.

Следующий прирост вероятнее всего даст точечное ускорение `/api/data` и холодных категорий после снижения лишнего polling. Для этого нужны отдельный live-профиль этих маршрутов, штатный `ANALYZE` в согласованное окно, затем планы фактических slow SQL. FCP/LCP/INP ещё не измерены; проба 1/10/50 выше относится только к `/api/stores` и не характеризует остальные страницы. p95 после изменения подтверждён только для `/api/stores` на малой выборке, выдавать его за показатель всех страниц нельзя.

## Предыдущий замер — 24 сентября 2026

Цель — быстрые интерактивные страницы во время синхронизации. Эта таблица показывает **один последовательный read-only замер работающего локального сервера** `127.0.0.1:4317` до включения правок этого этапа. Проверялся один и тот же локальный HTTP-сеанс; тело ответа считывалось, но не сохранялось. Даты источника — 16–22 сентября 2026 года. Это наблюдения, а не p95 и не тест под нагрузкой. Скрипт воспроизведения: `node scripts/measure-pult-http.cjs`.

Ни один из перечисленных маршрутов намеренно не вызывал Ozon/WB/TrueStats API: замер охватывал локальные чтения. Для отдельных случаев `GET /api/profit-series`, `/api/conversion` и `/api/economics/compare` прежний код всё же мог обращаться к TrueStats при пустом кеше; они не запускались в этом замере. В новом коде HTTP-читатели TrueStats берут только локальный кеш и ставят отдельное фоновое обновление.

## TOP‑20 по наблюдаемому времени

`После` и `×` оставлены «не измерено» для маршрутов, не прошедших повторный сопоставимый последовательный прогон после перезапуска. Живой замер `/api/stores` приведён выше отдельно. Изменения одного запроса нельзя переносить на другие запросы или выдавать синтетический тест за live‑ускорение.

| № | Операция | Сейчас, мс | Причина / состояние диагноза | Что исправлено в коде | После, мс | Ускорение |
| ---: | --- | ---: | --- | --- | ---: | ---: |
| 1 | Категории за неделю | 18 719 | Холодное чтение больших дневных источников; сборка проходила строки за пределами выбранной недели | Источники читаются после начальной ревизии; строки вне периода отсеиваются с сохранением поздних исправлений заказов | не измерено | не измерено |
| 2 | Продажи категорий за неделю | 17 347 | Каталог и финансовые источники читались последовательно; товары повторно фильтровались по каждому магазину | Чтение перекрыто, товары индексируются один раз, невыбранные магазины не читаются | не измерено | не измерено |
| 3 | Рейтинг товаров покупателей за 28 дней | 6 229 | Из SQL в Node переносится большой набор исходных строк; объём предыдущего профильного замера — около 120 МБ | Предрасчёт не внедрён: нужна отдельная проверка равенства дедупликации и последовательного округления | не измерено | не измерено |
| 4 | Рейтинг товаров покупателей за неделю | 5 810 | Тот же транспорт большого числа строк; доля SQL/JS пока не измерена по запросу | Загрузка по запросу; страницы одного SQL-источника читаются в одной read-only транзакции | не измерено | не измерено |
| 5 | Дневная иерархия категорий | 5 281 | Запрашивались лишние `orders.daily`, `types`, `errors` вместе с нужными SKU-полями | Добавлена узкая проекция category insights с сохранённым fallback | не измерено | не измерено |
| 6 | Состав покупателей за 28 дней | 3 736 | В прежнем профильном замере передавалось около 72 МБ исходных документов | Запуск только по явному действию в разделе; вычисление без смены семантики | не измерено | не измерено |
| 7 | Состав покупателей за неделю | 2 726 | Большие исходные документы; вклад SQL/JS ещё не разложен для этого периода | Отчёт отделён от коротких GET; транзакционный N+1 внутри исходных документов сокращён | не измерено | не измерено |
| 8 | Категории за 28 дней | 1 969 | Чтение дневных источников; один тёплый замер после недельного отчёта | Источники читаются параллельно после начальной ревизии | не измерено | не измерено |
| 9 | Полные insights за день | 1 228 | Полный режим читает каталог, ledger, цены и себестоимость для каждого Ozon-магазина; точная доля этапов пока неизвестна | Оставлен вне горячей очереди заказов; иной расчёт не менялся | не измерено | не измерено |
| 10 | Полные insights за неделю | 1 144 | Тот же полный режим и локальная модель отчёта | Не изменялось | не измерено | не измерено |
| 11 | Заказы за день | 193 | Локальный снимок и метаданные задач; это единичное чтение | Не изменялось | не измерено | не измерено |
| 12 | Каталог магазинов | 183 | Локальное чтение каталога; прежний частый опрос уже сокращён на предыдущем этапе | Дублирование двух навигационных событий больше не создаёт второй GET | не измерено | не измерено |
| 13 | Источники insights | 121 | Ответ 348 КБ; точная доля SQL/сериализации не измерена | Не изменялось | не измерено | не измерено |
| 14 | Заказы за неделю | 33 | Не установлена; операция уже короткая в этой пробе | Не изменялось | не измерено | не измерено |
| 15 | Заказы за 28 дней | 27 | Не установлена; операция уже короткая в этой пробе | Не изменялось | не измерено | не измерено |
| 16 | Регламент обновлений | 21 | Не установлена; операция уже короткая в этой пробе | Не изменялось | не измерено | не измерено |
| 17 | Состав покупателей за день | 15 | Не установлена; операция уже короткая в этой пробе | Не изменялось | не измерено | не измерено |
| 18 | Журнал изменений | 11 | Ответ 53 КБ; операция короткая в этой пробе | Не изменялось | не измерено | не измерено |
| 19 | Рейтинг покупателей за день | 8 | Пустой/неподготовленный результат, 373 байта — не показатель полной загрузки | Не изменялось | не измерено | не измерено |
| 20 | HTML главной | 3 | Только HTML, без ожидания данных экрана | Buyer-only JS/CSS перенесены на открытие соответствующего раздела | не измерено | не измерено |

## Проверенное в коде и оставшиеся измерения

- HTTP-метрики для **каждого недавнего запроса**: полное время, число и суммарное время SQL-запросов, ожидание пула, число и время вызовов через `fetchFn`, размер тела ответа. `GET /api/runtime-metrics` отдаёт ограниченную историю, агрегаты и p50/p95/p99 по маршрутам, плюс `total/idle/inUse/waiting` каждого пула. Код включён в рабочем процессе; для `/api/stores` и `/api/data` данные сняты выше. В метриках нет SQL-текста, параметров, query string, cookies или содержимого ответа.
- Пулы создаются один раз при старте: runtime `max=10`, UI `max=3`, analytics `max=2`, outbound `max=2`. Диагностические снимки `pg_stat_activity` в старых сериях показывали отдельные периоды ожидания UI; повторный замер со смешанными аналитическими чтениями записан выше. Расширять общий лимит без повторного профиля PostgreSQL не стали.
- Исторические HTTP-отчёты получили отдельную очередь по одному запросу. Тест доказал, что выполняющийся отчёт не блокирует короткий GET в другой очереди. Ускорение при реальной параллельной нагрузке ещё не измерено.
- Buyer-only JS/CSS исключены из начальной страницы: минус 2 запроса и 23 865 байт без сжатия (около 6 407 байт gzip). Дублирующиеся события навигации теперь дают один `/api/stores` вместо двух. Это измерения размера и тест поведения, не замер LCP.
- Синтетический stage-тест без live-сервера: `category-sales` 251,7 → 141,4 мс (1,78×) при заданных задержках источников. Спекулятивное чтение `order-category-daily` удалено после воспроизведения гонки кеша; прежний синтетический выигрыш этого участка более не заявляется.
- Прямой read-only замер нового обработчика `/api/order-categories` на фиксированном дне: медиана 1 156,8 → 424,6 мс (2,72×). Полные ответы совпали по структуре, размеру (142 499 байт) и SHA-256. Это прямой вызов, **не** замер включённого HTTP-сервера.
- Финальная прямая read-only проба недельного источника покупателей: 10 106 → 9 096 мс (1,11×), 195 → 60 SQL-команд, 2 399 → 2 116 мс суммарного SQL-времени. Число прочитанных снимков осталось равным четырём. Повторное тёплое чтение — 4,8–4,9 мс / 3 SQL. Основные оставшиеся секунды уходят на перенос и разбор больших строк; эти результаты не заменяют HTTP-замер.
- На тех же локально прочитанных документах сборка недельной динамики категорий сравнивалась по пять раз после прогрева: медиана 1 153,5 → 851,6 мс (1,35×). Полный ответ совпал (`deepStrictEqual`, 307 170 байт и одинаковый SHA-256). Внепериодные строки исключаются после учёта новых ревизий заказа; загрузка самих документов всё ещё занимает около десяти секунд. Это замер чистой сборки, не всего HTTP-маршрута.
- Для локальных источников зафиксированы SQL counts: каталоги — 38 запросов / 131,8 мс полного чтения; insights — 95 / 119,7 мс; категории insights — 47 / 85,6 мс; WB orders — 14 / 19,5 мс; buyer snapshots за неделю — 195 / 12 398,8 мс; все товары — 38 / 644,4 мс. При повторном независимом прямом чтении тех же buyer snapshots в одном процессе: холодный вызов 10 058,1 мс / 195 SQL, сразу повторенный тёплый — 5,0 мс / 3 SQL. Это разовые прямые замеры, а не количество запросов готового экрана или гарантированная работа кеша после синхронизации. На холодном проходе суммарное SQL-время было 2 270,2 мс, поэтому перенос и разбор исходных строк в Node остаётся подтверждённым крупным вкладом.
- Инкрементальный Ozon orders уже работает для текущего дня; полная загрузка и воронка запускаются по требованию. Исправлено возобновление legacy Ozon funnel после временного сбоя: сохранённые страницы больше не запрашиваются заново. PostgreSQL funnel collector пока не имеет durable page cursor — ограничение для следующего этапа.
- `pg_stat_statements` на рабочей базе не установлен. Индексы и настройки PostgreSQL не менялись: планы и замеры конкретных slow queries до/после пока не доказали необходимость нового индекса. Redis и новые сервисы не добавлялись.

После включения версии остаётся повторить скрипт в сопоставимых условиях для категорий и покупателей, заполнить SQL count/time для этих экранов, проверить свежесть и равенство финансовых ответов, затем вписать реальные значения в `После` и `×`. Время frontend rendering не подключено к конкретному HTTP-запросу: браузер собирает его отдельно, но без измерения нельзя утверждать достигнутую скорость отрисовки.
