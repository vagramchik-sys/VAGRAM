'use strict';
const { createJsonDocumentRepository } = require('../postgres-json-repository.cjs');
const { sourceKey } = require('../postgres-document-import.cjs');
const SOURCE = 'runtime-schedules.json', KEY = sourceKey(SOURCE), STATES = new Set(['queued','running','partial','done','error','unknown']);
const UUID=/^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/iu,HASH=/^[a-f0-9]{64}$/u;
class SchedulerError extends Error { constructor(code,message){super(message);this.name='SchedulerError';this.code=code;} }
const object=v=>v&&typeof v==='object'&&!Array.isArray(v);
function valid(value){return object(value)&&value.version===1&&object(value.jobs)&&(value.requests===undefined||object(value.requests)&&Object.entries(value.requests).every(([key,r])=>key===r.commandId&&UUID.test(key)&&r.kind==='insights-full'&&Number.isFinite(Date.parse(r.timestamp))&&Array.isArray(r.targets)&&r.targets.length<=1000&&r.targets.every((target,index)=>object(target)&&/^[0-9]+$/u.test(target.storeId)&&/^(?:0|[1-9][0-9]*)$/u.test(target.documentRevision)&&(index===0||r.targets[index-1].storeId.localeCompare(target.storeId)<0))))&&Object.entries(value.jobs).every(([key,j])=>key===`${j.kind}:${j.storeId}`&&/^[a-z][a-z0-9-]{0,40}$/u.test(j.kind)&&/^(?:wb-)?[0-9]+$/u.test(j.storeId)&&STATES.has(j.state)&&UUID.test(j.attemptId)&&UUID.test(j.commandId)&&Number.isFinite(Date.parse(j.timestamp))&&Number.isFinite(Date.parse(j.nextDueAt))&&Array.isArray(j.errorCodes)&&(j.runnerId==null||typeof j.runnerId==='string')&&(j.resolution==null||j.resolution?.outcome==='not-committed'&&UUID.test(j.resolution.attemptId))&&(j.kind==='derived-capture'?object(j.payload)&&HASH.test(j.payloadHash||''):j.payload===undefined&&j.payloadHash===undefined));}
function createPostgresScheduler({stateStore,statusReader=null,repository=createJsonDocumentRepository({stateStore,logicalKey:KEY,sourcePath:SOURCE,validate:valid})}={}){
  if(!repository?.read||!repository?.compareAndSet||!repository?.readCommand)throw new TypeError('scheduler repository is required');
  async function load(){const row=await repository.read();return row&&!row.deleted?{value:structuredClone(row.value),revision:row.revision}:{value:{version:1,jobs:{}},revision:'0'};}
  async function transition({kind,storeId,expectedRevision,commandId,attemptId,timestamp,nextDueAt,state,stage=null,count=0,errorCodes=[],documentRevision=null,runnerId=null,expectedAttemptId,expectedState,expectedRunnerId,resolution=null,payload,payloadHash}={}){
    const intent={kind,storeId,attemptId,commandId,timestamp,nextDueAt,state,stage,count,errorCodes,documentRevision,runnerId,resolution,...(payload===undefined?{}:{payload,payloadHash})};
    const prior=await repository.readCommand(commandId);if(prior){const before=prior.before.value?.jobs?.[`${kind}:${storeId}`],got=prior.after.value.jobs[`${kind}:${storeId}`];if(prior.before.revision!==String(expectedRevision)||expectedAttemptId!==undefined&&before?.attemptId!==expectedAttemptId||expectedState!==undefined&&before?.state!==expectedState||expectedRunnerId!==undefined&&(before?.runnerId??null)!==expectedRunnerId||JSON.stringify(got)!==JSON.stringify(intent))throw new SchedulerError('COMMAND_ID_REUSED','Scheduler command intent differs');return{revision:prior.after.revision,replayed:true,job:structuredClone(got)};}
    if(!valid({version:1,jobs:{[`${kind}:${storeId}`]:intent}})||!Number.isSafeInteger(count)||count<0||!errorCodes.every(v=>typeof v==='string'&&/^[A-Z0-9_]{1,80}$/u.test(v)))throw new SchedulerError('INVALID_ARGUMENT','Scheduler transition is invalid');
    const loaded=await load(),current=loaded.value.jobs[`${kind}:${storeId}`];
    if(expectedAttemptId!==undefined&&current?.attemptId!==expectedAttemptId||expectedState!==undefined&&current?.state!==expectedState||expectedRunnerId!==undefined&&(current?.runnerId??null)!==expectedRunnerId)throw new SchedulerError('JOB_CONFLICT','Scheduler job changed concurrently');
    if(current?.state==='unknown'&&state==='running')throw new SchedulerError('UNKNOWN_OUTCOME_HOLD','Unknown attempt requires same-command resolution');
    if(current?.state==='unknown'&&state==='queued'&&!(resolution?.attemptId===current.attemptId&&resolution?.outcome==='not-committed'))throw new SchedulerError('UNKNOWN_OUTCOME_HOLD','Unknown attempt requires explicit resolution');
    const next=structuredClone(loaded.value);next.jobs[`${kind}:${storeId}`]=intent;
    const result=await repository.compareAndSet(next,{expectedRevision,commandId});return{...result,job:structuredClone(intent)};
  }
  async function statusJobs(){return statusReader?statusReader():((await load()).value.jobs)}
  async function jobsProvider(){const jobs=await statusJobs(),selected=new Map();for(const j of Object.values(jobs)){const prior=selected.get(j.storeId);if(!prior||j.state==='running'&&prior.state!=='running'||j.timestamp>prior.timestamp)selected.set(j.storeId,j);}return Object.fromEntries([...selected].map(([id,j])=>[id,{status:j.state,stage:j.stage,count:j.count,startedAt:j.state==='running'?j.timestamp:null,finishedAt:['done','partial','error'].includes(j.state)?j.timestamp:null,nextDueAt:j.nextDueAt}]));}
  async function captureRequest({commandId,timestamp,kind='insights-full',targets}={}){commandId=typeof commandId==='string'?commandId.toLowerCase():commandId;const intent={commandId,timestamp,kind,targets:Array.isArray(targets)?structuredClone(targets).sort((a,b)=>a.storeId.localeCompare(b.storeId)):targets};if(!valid({version:1,jobs:{},requests:{[commandId]:intent}}))throw new SchedulerError('INVALID_ARGUMENT','Scheduler request group is invalid');for(let retry=0;retry<3;retry++){const prior=await repository.readCommand(commandId);if(prior){const saved=prior.after.value.requests?.[commandId];if(!saved||saved.commandId!==commandId||saved.timestamp!==timestamp||saved.kind!==kind)throw new SchedulerError('COMMAND_ID_REUSED','Scheduler request group intent differs');return{targets:structuredClone(saved.targets),replayed:true,revision:prior.after.revision}}const loaded=await load(),next=structuredClone(loaded.value);next.requests=object(next.requests)?next.requests:{};next.requests[commandId]=intent;const entries=Object.values(next.requests).sort((a,b)=>b.timestamp.localeCompare(a.timestamp)||b.commandId.localeCompare(a.commandId));next.requests=Object.fromEntries(entries.slice(0,200).map(row=>[row.commandId,row]));try{const result=await repository.compareAndSet(next,{expectedRevision:loaded.revision,commandId});return{targets:structuredClone(intent.targets),replayed:false,revision:result.revision}}catch(error){if(error?.code!=='REVISION_CONFLICT'||retry===2)throw error;}}}
  async function transitionReceipt({commandId,kind,storeId,timestamp}={}){const prior=await repository.readCommand(commandId);if(!prior)return null;const saved=prior.after.value.jobs?.[`${kind}:${storeId}`];if(!saved||saved.commandId!==commandId||saved.kind!==kind||saved.storeId!==storeId||saved.timestamp!==timestamp)throw new SchedulerError('COMMAND_ID_REUSED','Scheduler transition intent differs');return structuredClone(saved)}
  return Object.freeze({load,transition,captureRequest,transitionReceipt,jobsProvider,statusJobs,sourcePath:SOURCE});
}
module.exports={createPostgresScheduler,SchedulerError};
