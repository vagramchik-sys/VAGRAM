'use strict';
const OPTIMIZER_SCHEMA_SQL=String.raw`CREATE SCHEMA IF NOT EXISTS pult_optimizer;
CREATE TABLE IF NOT EXISTS pult_optimizer.credentials(store_id text COLLATE "C" PRIMARY KEY,client_id_ciphertext text NOT NULL,client_secret_ciphertext text NOT NULL,credential_version uuid NOT NULL,revision bigint NOT NULL CHECK(revision>0),created_at timestamptz NOT NULL,updated_at timestamptz NOT NULL,last_test_at timestamptz,last_test_status text,last_error_code text CHECK(last_error_code IS NULL OR last_error_code~'^[A-Z0-9_]+$'));
CREATE TABLE IF NOT EXISTS pult_optimizer.commands(command_id uuid PRIMARY KEY,kind text COLLATE "C" NOT NULL,store_id text COLLATE "C",intent_hash text NOT NULL CHECK(intent_hash~'^[a-f0-9]{64}$'),receipt jsonb NOT NULL CHECK(jsonb_typeof(receipt)='object' AND octet_length(receipt::text)<=16384),created_at timestamptz NOT NULL DEFAULT clock_timestamp());
CREATE INDEX IF NOT EXISTS optimizer_commands_store_idx ON pult_optimizer.commands(store_id,created_at DESC);
CREATE TABLE IF NOT EXISTS pult_optimizer.refresh_state(store_id text COLLATE "C" PRIMARY KEY,revision bigint NOT NULL CHECK(revision>=0),credential_version uuid,status text NOT NULL CHECK(status IN('never','running','ready','partial','error')),last_attempt_at timestamptz,last_success_at timestamptz,retry_at timestamptz,last_error_code text CHECK(last_error_code IS NULL OR last_error_code~'^[A-Z0-9_]+$'),last_command_id uuid,campaigns_observed_at timestamptz,products_observed_at timestamptz,statistics_observed_at timestamptz,campaigns_count integer NOT NULL DEFAULT 0 CHECK(campaigns_count>=0),products_count integer NOT NULL DEFAULT 0 CHECK(products_count>=0),statistics_count integer NOT NULL DEFAULT 0 CHECK(statistics_count>=0),updated_at timestamptz NOT NULL DEFAULT clock_timestamp());
CREATE TABLE IF NOT EXISTS pult_optimizer.campaigns(store_id text COLLATE "C" NOT NULL,campaign_id text COLLATE "C" NOT NULL,name text,state text,payment_type text,strategy text,currency text,active boolean,observed_at timestamptz NOT NULL,source_updated_at timestamptz,source_revision bigint NOT NULL CHECK(source_revision>0),PRIMARY KEY(store_id,campaign_id));
CREATE INDEX IF NOT EXISTS optimizer_campaigns_state_idx ON pult_optimizer.campaigns(store_id,active,state,campaign_id);
CREATE TABLE IF NOT EXISTS pult_optimizer.campaign_products(store_id text COLLATE "C" NOT NULL,campaign_id text COLLATE "C" NOT NULL,sku text COLLATE "C" NOT NULL,product_id text COLLATE "C",current_bid numeric(20,6),competitive_bid numeric(20,6),minimum_bid numeric(20,6),bid_unit text,current_bid_raw text,competitive_bid_raw text,minimum_bid_raw text,current_bid_raw_unit text,competitive_bid_raw_unit text,minimum_bid_raw_unit text,status text,observed_at timestamptz NOT NULL,competitive_observed_at timestamptz,minimum_observed_at timestamptz,source_revision bigint NOT NULL CHECK(source_revision>0),PRIMARY KEY(store_id,campaign_id,sku),FOREIGN KEY(store_id,campaign_id) REFERENCES pult_optimizer.campaigns(store_id,campaign_id) ON DELETE CASCADE);
CREATE INDEX IF NOT EXISTS optimizer_products_sku_idx ON pult_optimizer.campaign_products(store_id,sku,campaign_id);
CREATE INDEX IF NOT EXISTS optimizer_products_product_idx ON pult_optimizer.campaign_products(store_id,product_id,campaign_id);
CREATE TABLE IF NOT EXISTS pult_optimizer.statistics(store_id text COLLATE "C" NOT NULL,campaign_id text COLLATE "C" NOT NULL,sku text COLLATE "C" NOT NULL,stat_date date NOT NULL,impressions bigint CHECK(impressions IS NULL OR impressions>=0),clicks bigint CHECK(clicks IS NULL OR clicks>=0),orders bigint CHECK(orders IS NULL OR orders>=0),spend numeric(20,6),revenue numeric(20,6),currency text,order_basis text,complete boolean NOT NULL DEFAULT true,observed_at timestamptz NOT NULL,source_revision bigint NOT NULL CHECK(source_revision>0),PRIMARY KEY(store_id,campaign_id,sku,stat_date));
CREATE INDEX IF NOT EXISTS optimizer_statistics_sku_idx ON pult_optimizer.statistics(store_id,sku,stat_date DESC,campaign_id);
CREATE INDEX IF NOT EXISTS optimizer_statistics_campaign_idx ON pult_optimizer.statistics(store_id,stat_date DESC,campaign_id,sku);
CREATE TABLE IF NOT EXISTS pult_optimizer.sku_statistics_periods(store_id text COLLATE "C" NOT NULL,sku text COLLATE "C" NOT NULL,period_from date NOT NULL,period_to date NOT NULL,attribution_model text COLLATE "C" NOT NULL,impressions bigint,clicks bigint,orders bigint,spend numeric(20,6),revenue numeric(20,6),currency text,order_basis text,complete boolean NOT NULL DEFAULT false,observed_at timestamptz NOT NULL,source_revision bigint NOT NULL CHECK(source_revision>0),PRIMARY KEY(store_id,sku,period_from,period_to,attribution_model));
CREATE INDEX IF NOT EXISTS optimizer_statistics_periods_sku_idx ON pult_optimizer.sku_statistics_periods(store_id,sku,period_to DESC);
CREATE TABLE IF NOT EXISTS pult_optimizer.sku_links(store_id text COLLATE "C" NOT NULL,sku text COLLATE "C" NOT NULL,product_id text COLLATE "C",status text NOT NULL CHECK(status IN('matched','unmapped','ambiguous')),source_revision bigint NOT NULL CHECK(source_revision>0),observed_at timestamptz NOT NULL,PRIMARY KEY(store_id,sku));
CREATE INDEX IF NOT EXISTS optimizer_sku_links_product_idx ON pult_optimizer.sku_links(store_id,product_id,sku);
CREATE TABLE IF NOT EXISTS pult_optimizer.settings(scope_key text COLLATE "C" PRIMARY KEY CHECK(scope_key='global' OR scope_key~'^store:[0-9]+$'),revision bigint NOT NULL CHECK(revision>0),settings jsonb NOT NULL CHECK(jsonb_typeof(settings)='object' AND octet_length(settings::text)<=16384),updated_at timestamptz NOT NULL);
CREATE TABLE IF NOT EXISTS pult_optimizer.experiments(experiment_id uuid PRIMARY KEY,store_id text COLLATE "C" NOT NULL,product_id text COLLATE "C" NOT NULL,campaign_id text COLLATE "C",sku text COLLATE "C",dimension text NOT NULL CHECK(dimension IN('PRICE','BID')),status text NOT NULL CHECK(status IN('recorded','observing','completed','cancelled')),before_value numeric(20,6),after_value numeric(20,6),baseline_contribution numeric(20,6),result_contribution numeric(20,6),period_from date,period_to date,started_at timestamptz NOT NULL,observe_until timestamptz,closed_at timestamptz,revision bigint NOT NULL CHECK(revision>0),command_id uuid NOT NULL UNIQUE);
CREATE INDEX IF NOT EXISTS optimizer_experiments_product_idx ON pult_optimizer.experiments(store_id,product_id,started_at DESC);
CREATE UNIQUE INDEX IF NOT EXISTS optimizer_experiments_active_idx ON pult_optimizer.experiments(store_id,product_id) WHERE status IN('recorded','observing');
CREATE TABLE IF NOT EXISTS pult_optimizer.decisions(decision_id uuid PRIMARY KEY,store_id text COLLATE "C" NOT NULL,product_id text COLLATE "C" NOT NULL,campaign_id text COLLATE "C",sku text COLLATE "C",input_hash text NOT NULL CHECK(input_hash~'^[a-f0-9]{64}$'),algorithm_version text NOT NULL,settings_revision bigint NOT NULL,source_revisions jsonb NOT NULL CHECK(jsonb_typeof(source_revisions)='object'),state text NOT NULL,action text NOT NULL,recommended_price numeric(20,6),recommended_bid numeric(20,6),max_profitable_bid numeric(20,6),confidence text,reason_codes jsonb NOT NULL CHECK(jsonb_typeof(reason_codes)='array'),blockers jsonb NOT NULL CHECK(jsonb_typeof(blockers)='array'),human_reason text,observed_at timestamptz NOT NULL);
CREATE UNIQUE INDEX IF NOT EXISTS optimizer_decisions_input_idx ON pult_optimizer.decisions(store_id,product_id,COALESCE(campaign_id,''),COALESCE(sku,''),input_hash,algorithm_version,settings_revision);
CREATE INDEX IF NOT EXISTS optimizer_decisions_product_idx ON pult_optimizer.decisions(store_id,product_id,observed_at DESC);
INSERT INTO pult_optimizer.settings(scope_key,revision,settings,updated_at) VALUES('global',1,'{"mode":"OBSERVE","killSwitch":false,"targetProfitPerOrder":0,"priceStepPct":0.05,"bidStepPct":0.1,"competitiveBuffer":1,"safetyFactor":0.8,"minStock":5,"minStockDays":7,"maxPriceAgeHours":24,"maxAdsAgeHours":2,"minImpressions":1000,"minClicks":100,"minOrders":10,"observationDays":7,"cooldownHours":72}'::jsonb,clock_timestamp()) ON CONFLICT(scope_key) DO NOTHING;
REVOKE ALL ON SCHEMA pult_optimizer FROM PUBLIC;REVOKE ALL ON ALL TABLES IN SCHEMA pult_optimizer FROM PUBLIC;REVOKE ALL ON ALL SEQUENCES IN SCHEMA pult_optimizer FROM PUBLIC;`;
async function ensureOptimizerSchema(queryable){if(!queryable?.query)throw new TypeError('queryable is required');await queryable.query(OPTIMIZER_SCHEMA_SQL)}
const OPTIMIZER_REQUIRED_COLUMNS=Object.freeze({
 credentials:['store_id','client_id_ciphertext','client_secret_ciphertext','credential_version','revision','created_at','updated_at','last_test_at','last_test_status','last_error_code'],
 commands:['command_id','kind','store_id','intent_hash','receipt','created_at'],
 refresh_state:['store_id','revision','credential_version','status','last_attempt_at','last_success_at','retry_at','last_error_code','last_command_id','campaigns_observed_at','products_observed_at','statistics_observed_at','campaigns_count','products_count','statistics_count','updated_at'],
 campaigns:['store_id','campaign_id','name','state','payment_type','strategy','currency','active','observed_at','source_updated_at','source_revision'],
 campaign_products:['store_id','campaign_id','sku','product_id','current_bid','competitive_bid','minimum_bid','bid_unit','current_bid_raw','competitive_bid_raw','minimum_bid_raw','current_bid_raw_unit','competitive_bid_raw_unit','minimum_bid_raw_unit','status','observed_at','competitive_observed_at','minimum_observed_at','source_revision'],
 statistics:['store_id','campaign_id','sku','stat_date','impressions','clicks','orders','spend','revenue','currency','order_basis','complete','observed_at','source_revision'],
 sku_statistics_periods:['store_id','sku','period_from','period_to','attribution_model','impressions','clicks','orders','spend','revenue','currency','order_basis','complete','observed_at','source_revision'],
 sku_links:['store_id','sku','product_id','status','source_revision','observed_at'],
 settings:['scope_key','revision','settings','updated_at'],
 experiments:['experiment_id','store_id','product_id','campaign_id','sku','dimension','status','before_value','after_value','baseline_contribution','result_contribution','period_from','period_to','started_at','observe_until','closed_at','revision','command_id'],
 decisions:['decision_id','store_id','product_id','campaign_id','sku','input_hash','algorithm_version','settings_revision','source_revisions','state','action','recommended_price','recommended_bid','max_profitable_bid','confidence','reason_codes','blockers','human_reason','observed_at']
});
async function optimizerSchemaReadiness(queryable){
 if(!queryable?.query)throw new TypeError('queryable is required');
 const result=await queryable.query("SELECT table_name,column_name FROM information_schema.columns WHERE table_schema='pult_optimizer' ORDER BY table_name,column_name");
 const present=new Set((result.rows||[]).map(row=>`${row.table_name}.${row.column_name}`));
 const missing=[];for(const[table,columns]of Object.entries(OPTIMIZER_REQUIRED_COLUMNS))for(const column of columns)if(!present.has(`${table}.${column}`))missing.push(`${table}.${column}`);
 return Object.freeze({ready:missing.length===0,code:missing.length?'OPTIMIZER_SCHEMA_MISSING':null,missing:Object.freeze(missing)});
}
async function optimizerRuntimeReadiness(queryable){
 const schema=await optimizerSchemaReadiness(queryable);if(!schema.ready)return schema;
 const access=(await queryable.query("SELECT has_schema_privilege(current_user,'pult_optimizer','USAGE') AS schema_usage,has_table_privilege(current_user,'pult_optimizer.settings','SELECT') AND has_table_privilege(current_user,'pult_optimizer.settings','INSERT') AND has_table_privilege(current_user,'pult_optimizer.settings','UPDATE') AS settings_write,has_table_privilege(current_user,'pult_optimizer.commands','SELECT') AND has_table_privilege(current_user,'pult_optimizer.commands','INSERT') AS commands_append,has_table_privilege(current_user,'pult_optimizer.commands','UPDATE') OR has_table_privilege(current_user,'pult_optimizer.commands','DELETE') AS commands_rewrite")).rows?.[0];
 const ready=Boolean(access?.schema_usage&&access.settings_write&&access.commands_append&&!access.commands_rewrite);return Object.freeze({ready,code:ready?null:'OPTIMIZER_PRIVILEGES_INVALID',missing:Object.freeze([])});
}
module.exports={OPTIMIZER_SCHEMA_SQL,OPTIMIZER_REQUIRED_COLUMNS,ensureOptimizerSchema,optimizerSchemaReadiness,optimizerRuntimeReadiness};
