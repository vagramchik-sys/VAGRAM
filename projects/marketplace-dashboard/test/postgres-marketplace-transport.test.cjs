'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{createOzonApi,createWbApi}=require('../storage/acquisition/postgres-marketplace-transport.cjs');
test('credential transports reject redirects and keep approved hosts',async()=>{const seen=[],fetchFn=async(url,options)=>{seen.push([url,options]);return{ok:true,status:200,json:async()=>({ok:true}),headers:{get:()=>null}}};await createOzonApi({fetchFn})({clientId:'1'},'secret','/v3/product/list',{});await createWbApi({fetchFn})('secret','content','/content/v2/get/cards/list',{});assert.equal(seen[0][0],'https://api-seller.ozon.ru/v3/product/list');assert.equal(seen[1][0],'https://content-api.wildberries.ru/content/v2/get/cards/list');assert.ok(seen.every(([,options])=>options.redirect==='error'));assert.equal(JSON.stringify(seen).includes('http://evil'),false)});
