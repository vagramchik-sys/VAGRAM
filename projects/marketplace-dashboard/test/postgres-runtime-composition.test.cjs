'use strict';const test=require('node:test'),assert=require('node:assert/strict');const{createPostgresRuntime}=require('../storage/postgres-runtime-composition.cjs');
test('partial composition stays gated and owner adapters cannot replace root authorize',async()=>{let loaded=0,captured,receivedAuthorize;const stateStore={read:async()=>null,list(){},write(){},remove(){},readCommand(){}};const runner={start(){},close(){}},forged=async()=>true,rootAuthorize=async()=>false;const runtime=createPostgresRuntime({pool:{query:async()=>({rows:[]}),connect(){}},stateStore,scheduler:{load:async()=>{loaded++},jobsProvider:async()=>({})},schedulerRunner:runner,ownerAdapters:{authorize:forged},ownerRoutesBuilder:options=>{receivedAuthorize=options.authorize;return{handle(){}}},acquisition:{market:{acquire(){}},costsPrices:{refresh(){}},insights:{refreshOrders(){},refreshFunnel(){}}},staticDir:'C:\\public',staticFiles:['index.html'],startServer:async options=>{captured=options;options.ownerRoutesFactory({authorize:rootAuthorize});return{close(){}}}});assert.deepEqual(await runtime.readiness(),{ready:false,missingAdapters:['runtime-composition'],passive:false});await runtime.start({port:0});assert.equal(receivedAuthorize,rootAuthorize);assert.notEqual(receivedAuthorize,forged);assert.equal(captured.background[0],runner);assert.equal(captured.port,0);assert.ok(loaded>=1);});
