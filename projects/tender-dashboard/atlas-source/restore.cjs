const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');
const destination=process.argv[2];if(!destination)throw Error('Укажите новую пустую папку');const root=path.resolve(destination);if(fs.existsSync(root)&&fs.readdirSync(root).length)throw Error('Папка должна быть пустой');const source=JSON.parse(fs.readFileSync(path.join(__dirname,'source.json'),'utf8'));const planned=[];
for(const [name,content] of Object.entries(source.files)){if(name.includes('\\')||path.isAbsolute(name)||name.split('/').some(p=>!p||p==='.'||p==='..'))throw Error('Небезопасный путь');const file=path.resolve(root,name);if(!file.startsWith(root+path.sep))throw Error('Путь вне папки');if(crypto.createHash('sha256').update(content).digest('hex')!==source.sha256[name])throw Error('Контрольная сумма не совпала');planned.push([file,content]);}
for(const [file,content] of planned){fs.mkdirSync(path.dirname(file),{recursive:true});fs.writeFileSync(file,content,{flag:'wx'});}console.log('Восстановлено '+planned.length+' файлов. Настройки подключения создаются отдельно.');
