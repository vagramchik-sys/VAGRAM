# Журнал улучшений

## 16 сентября 2026 — диагностика сбоев ИИ

Подготовлены безопасные коды ошибок: запуск Codex, завершение процесса без ответа, тайм-аут, превышение объёма и пустой ответ. Диалог показывает понятную причину и следующий шаг. Неизвестные ошибки получают общее сообщение; исходный текст исключений пользователю не передаётся.

Адаптер записывает в стандартный журнал ошибок только время, безопасный код и числовой код завершения процесса. Переписка, ключи и вывод провайдера не записываются. Для неизвестного завершения процесса точная сетевая или серверная причина по-прежнему может потребовать отдельной диагностики.

Проверено на отдельных тестовых базах: сценарии ошибок адаптера и отсутствие утечки текста; API диалогов с известной и неизвестной ошибкой; регрессии диспетчера и интерфейса диалогов. Все проверки прошли. Рабочая база не изменялась.

Резервная копия изменённых исходников: `work/backup-ai-errors-20260916-1415` относительно корня задачи.

**Применение:** требуется перезапуск работающего сервера. Он оставлен работающим на прежнем коде: отсутствие незавершённых пользовательских операций не подтверждено. Перед перезапуском проверить генерации, поручения и пользовательские формы; перезапуск завершит текущие сеансы входа.

Осталось: применить подготовленное изменение в безопасный момент и проверить отображение кода ошибки в браузере на отдельной базе. Автоматический повтор запросов не добавлялся, чтобы не дублировать обращения и расход лимита.

## 16 сентября 2026 — явный результат обращения

В диалоге теперь отдельно показаны состояния «Директор готовит ответ», «Ответ директора готов» и «Ответ не получен». Старое сообщение о принятии сервером заменено подсказкой смотреть итог в диалоге. Позднее подтверждение отправки другому директору не меняет верхний статус выбранного диалога.

Проверки: регрессионные тесты интерфейса, включая ошибку, успешный ответ и повторное ожидание после старой ошибки; изолированная страница на порту 8788 с тестовым AI_TIMEOUT проверена в браузере. Рабочая база не использовалась для тестирования. Сервер работает, активных генераций и поручений при проверке нет; в портфеле 0 проектов и 0 задач.

Резервная копия: `work/backup-director-status-20260916-1428`. Изменение JavaScript доступно после перезагрузки страницы; перезапуск сервера не требуется. Подготовленная ранее серверная диагностика всё ещё ожидает безопасного перезапуска. Проверка отображения её сообщения в браузере завершена на изолированном примере.

## 16 сентября 2026 — восстановление обращения после сбоя

Добавлена кнопка «Вернуть текст обращения» после неудачного ответа. Она восстанавливает последнее обращение и выбранный проект в черновик без отправки. Существующий другой черновик заменяется только после подтверждения. Недоступный проект сохраняется с понятной пометкой для выбора другого; наблюдателю кнопка не предлагается.

Проверено: регрессии интерфейса, отмена замены черновика, восстановление текста и контекста, отсутствие сетевой отправки при восстановлении. В изолированном браузере подтверждено заполнение поля исходным текстом. Рабочая база не менялась; проектов и задач по-прежнему нет, активных генераций и поручений при проверке нет.

Резервная копия: `work/backup-retry-draft-20260916-1440`. Кнопка станет доступна после обновления страницы. Серверная диагностика из первой записи остаётся подготовленной до безопасного перезапуска.

## 16 сентября 2026 — серверная диагностика применена

Завершено применение подготовленной диагностики: после повторных тестов адаптера и API сервер перезапущен. Перед остановкой подтверждены отсутствие активных генераций и поручений, пустые поля на двух страницах директоров и отсутствие ввода на странице входа. Перед самой остановкой повторно проверены рабочая база и идентификатор процесса.

Новый процесс: 43672, порт 8787. Страница входа возвращает HTTP 200, журнал ошибок запуска пуст. Контрольная сумма рабочей базы после перезапуска совпадает с резервной копией `work/store-before-diagnostics-apply.json`. Переписки, пользователи и данные не менялись. Сеансы входа завершились при перезапуске — требуется повторный вход.

Пункт ожидания перезапуска из предыдущих записей закрыт. Новые ошибки ответов будут получать безопасные коды. Проверка намеренной ошибки в рабочей базе не выполнялась; сценарии отказов проверены отдельно.

## 16 сентября 2026 — разблокировка восстановления при быстром сбое

Обнаружена и исправлена ошибка: если неудачный ответ уже получен при первом чтении истории после отправки, кнопка восстановления создавалась заблокированной и не включалась по завершении запроса. Теперь её состояние обновляется вместе с остальными элементами управления.

Добавлен тест мгновенного сбоя: до исправления он падал, после исправления прошёл вместе с регрессиями диалогов. В браузере на изолированном сервере выполнена тестовая отправка с немедленной ошибкой и подтверждена доступность кнопки восстановления. Реальный ИИ и рабочая база в тестах не использовались.

Резервная копия: `work/backup-restore-unlock-20260916-1504`. Сервер 43672 продолжает работать, перезапуск не нужен; исправление доступно после обновления страницы. При проверке портфель пуст, активных обращений и поручений нет. Следующая возможная проверка: поведение при потере соединения во время чтения истории.
